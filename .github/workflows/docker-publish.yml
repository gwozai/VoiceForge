name: Build and Publish Docker Image

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # 允许手动触发

env:
  REGISTRY: docker.io
  IMAGE_NAME: gwozai/voiceforge

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Get image digest
        id: digest
        if: github.event_name != 'pull_request'
        run: echo "digest=${{ steps.build.outputs.digest }}" >> $GITHUB_OUTPUT

      # 成功通知
      - name: Notify Feishu on Success
        if: success() && github.event_name != 'pull_request'
        env:
          FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}
          IMAGE_TAG: ${{ steps.meta.outputs.tags }}
        run: |
          if [ -z "$FEISHU_WEBHOOK" ]; then
            echo "FEISHU_WEBHOOK not set, skipping notification"
            exit 0
          fi
          curl -X POST "$FEISHU_WEBHOOK" \
            -H "Content-Type: application/json" \
            -d "{
              \"msg_type\": \"post\",
              \"content\": {
                \"post\": {
                  \"zh_cn\": {
                    \"title\": \"🎉 VoiceForge Docker 镜像发布成功\",
                    \"content\": [
                      [{\"tag\": \"text\", \"text\": \"镜像: gwozai/voiceforge\"}],
                      [{\"tag\": \"text\", \"text\": \"平台: linux/amd64, linux/arm64\"}],
                      [{\"tag\": \"text\", \"text\": \"分支: ${{ github.ref_name }}\"}],
                      [{\"tag\": \"text\", \"text\": \"\"}],
                      [{\"tag\": \"text\", \"text\": \"拉取命令:\"}],
                      [{\"tag\": \"text\", \"text\": \"docker pull gwozai/voiceforge:latest\"}],
                      [{\"tag\": \"text\", \"text\": \"\"}],
                      [{\"tag\": \"a\", \"text\": \"查看 Docker Hub\", \"href\": \"https://hub.docker.com/r/gwozai/voiceforge\"}]
                    ]
                  }
                }
              }
            }"

      # 失败通知
      - name: Notify Feishu on Failure
        if: failure()
        env:
          FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}
        run: |
          if [ -z "$FEISHU_WEBHOOK" ]; then
            echo "FEISHU_WEBHOOK not set, skipping notification"
            exit 0
          fi
          curl -X POST "$FEISHU_WEBHOOK" \
            -H "Content-Type: application/json" \
            -d "{
              \"msg_type\": \"post\",
              \"content\": {
                \"post\": {
                  \"zh_cn\": {
                    \"title\": \"❌ VoiceForge Docker 构建失败\",
                    \"content\": [
                      [{\"tag\": \"text\", \"text\": \"仓库: ${{ github.repository }}\"}],
                      [{\"tag\": \"text\", \"text\": \"分支: ${{ github.ref_name }}\"}],
                      [{\"tag\": \"text\", \"text\": \"触发者: ${{ github.actor }}\"}],
                      [{\"tag\": \"text\", \"text\": \"\"}],
                      [{\"tag\": \"a\", \"text\": \"查看错误日志\", \"href\": \"${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\"}]
                    ]
                  }
                }
              }
            }"
